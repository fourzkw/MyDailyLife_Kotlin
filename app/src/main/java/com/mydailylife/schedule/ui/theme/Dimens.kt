package com.mydailylife.schedule.ui.theme

import androidx.compose.ui.unit.dp

/** Horizontal inset shared by tab screens. */
val ScreenHorizontalPadding = 16.dp

/** Space below status-bar content inset, before the page title. */
val ScreenTopPadding = 8.dp

/** Space between page title ([SectionHeader] / TopAppBar) and first content. */
val ScreenHeaderToContent = 12.dp
